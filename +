<script lang="ts">
	import ArticleMetadata from '$lib/components/ArticleMetadata.svelte';
	import type { PageData } from './$types';

	export let data: PageData;

	$: article = data.data[0]
</script>

<div class="wrapper">
	{#if data.data[0].picture}

		<img src={data.data[0].picture.img} alt={article.title} />
	{/if}

	<ArticleMetadata {article} />
</div>

<style>
	.wrapper {
		position: relative;
		min-height: 100dvh;

		background: #ff00ff;
	}

	img {
		position: absolute;

		inset: 0;
		min-width: 100%;
		min-height: 100%;
		object-fit: cover;
	}
</style>
